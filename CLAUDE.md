# 项目 AI 协作规范

## 核心原则
1. **架构优先**：模块边界清晰，职责单一，低耦合高内聚。每次调用可预期。
2. **禁止过度设计**：不写兜底代码、冗余代码、猜测性代码。不明白的地方必须先询问。
3. **修改可控**：改动范围必须明确，未许可的文件/逻辑不得修改。
4. **可追溯**：每次修改后必须输出改动摘要，包含风险点和测试建议。

## 开发流程强制要求
- **复杂功能必须先出 plan**：plan 需包含影响文件、操作步骤、验证方法、风险点。
- **plan 不清晰不得写代码**：若无法用 plan 清晰描述，说明对需求理解不足。
- **每完成一个小功能，git commit 一次**：确保状态可回退。

## 代码风格与质量
- 接口和数据结构保持向后兼容，除非明确声明。
- 异常状态必须显式处理，不能静默忽略。
- 避免重复逻辑，抽取公共函数/模块。
- 命名清晰，注释简洁。

## 限制条件（AI 执行时自动遵守）
- 禁止修改未列入允许范围的文件（除非明确要求）。
- 禁止为了"能跑"而添加不必要的兼容代码或 fallback 逻辑。
- 禁止改动接口返回结构而不通知用户。
- 禁止新增未明确要求的功能。

## 输出要求
每次代码修改后，必须输出以下内容：
- 改动文件列表
- 每个文件的修改摘要
- 修改原因
- 潜在风险
- 建议的测试方法

## 沟通规范
- 遇到不明确的需求，必须先提问澄清，不要自行猜测。
- 当有多种实现方案时，简要列出优缺点，供用户选择。
- 对于可能破坏现有功能的改动，必须提前警告。
